import { FunctionComponent } from "react";
import styles from './block12.module.css';


interface Props {
}

const Block12: FunctionComponent<Props> = ({ }) => {
  return (
    <div className={styles["container"] + " contentWrapper"}>

    </div>
  );
};

export default Block12;